# ---- Development container for Vite + React (HMR) ----
FROM node:20-alpine
WORKDIR /app
RUN apk add --no-cache libc6-compat
# Polling makes file-watching reliable in Docker on macOS/Windows/WSL2
ENV CHOKIDAR_USEPOLLING=true
ENV WATCHPACK_POLLING=true
# Install deps separately to leverage Docker layer caching
COPY package*.json ./
RUN npm ci
# Copy the rest of the app (overridden by bind-mount in compose)
COPY . .
EXPOSE 5173
# Bind on all interfaces so host can access it
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
